import React from 'react';

const NavBar = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '80px',
        background: 'tomato',
        justifyContent: 'center',
        display: 'flex',
        alignItems: 'center',
        marginBottom: '10px',
        // padding: '0px 0px',
      }}
    >
      네브
    </div>
  );
};

export default NavBar;
